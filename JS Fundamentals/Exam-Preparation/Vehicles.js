/*jshint esversion: 6 */

function solve(args) {
    let totalWheels = +args[0];
    car = 4; truck = 10; trikes = 3;
    let count = 0;

    while(!totalWheels <= 0)
    {
        
    }
    // Your solution here
    console.log(count);
}
